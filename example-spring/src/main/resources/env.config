envType                 = pre

# 日常数据库(MySQL 20MB)
jdbc.database_host      = db.daily.hasor.net
jdbc.database_name      = hdm1250987_db
jdbc.user               = hdm1250987
jdbc.password           = s3G3j3f98t6Uqh46f67t

# 分布式服务框架 RSF
#
# 注册中心地址
RSF_CENTER_SERVERS      = rsf://127.0.0.1:2180


# RSF对外提供服务的IP（如果是正式部署，可以不进行下面配置。系统会取 local 本机网卡地址默认值）
RSF_SERVICE_IP          = 127.0.0.1
# RSF对外提供服务的服务端口（如果是正式部署，可以不进行下面配置。系统会取 2180 端口号作为默认值）
RSF_SERVICE_PORT        = 2170
# Telnet 控制台端口（如果是正式部署，可以不进行下面配置。系统会取 2181 端口号作为默认值）
RSF_CONSOLE_PORT        = 2171