env                           = pre

# 默认加载的包
HASOR_LOAD_PACKAGES           = net.demo.hasor.*

# WebMVC 拦截的请求类型
HASOR_RESTFUL_PATTERNS        = htm;html;do;json;

# 程序入口模块类名
HASOR_LOAD_STARTUP            = net.demo.hasor.core.StartModule

# Fileupload 允许的请求大小
HASOR_UPLOAD_MAX_REQUEST_SIZE = -1

# Fileupload 允许上传的单个文件大小
HASOR_UPLOAD_MAX_FILE_SIZE    = -1

# 工作目录
WORK_HOME                     = %HASOR_WEBROOT%/WEB-INF/classes/daily_home

# 日常数据库(MySQL 20MB)
jdbc.database_host          = db.daily.hasor.net
jdbc.database_name          = hdm1250987_db
jdbc.user                   = hdm1250987
jdbc.password               = s3G3j3f98t6Uqh46f67t

jdbcDriver

<jdbcDriver>com.mysql.jdbc.Driver</jdbcDriver>
<jdbcURL></jdbcURL>
<userName>${jdbc.user}</userName>
<userPassword>${jdbc.password}</userPassword>



jdbc:mysql://%jdbc.database_host%:3306/%jdbc.database_name%?useUnicode=true&characterEncoding=utf-8